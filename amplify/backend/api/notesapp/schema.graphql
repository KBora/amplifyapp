type Piece @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  composer: String
  duration: Int
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 